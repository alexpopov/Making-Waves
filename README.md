# Making Waves

Browser-based WAV slicer for sample prep (Digitakt 2, etc). Runs entirely client-side.

## Run

```
npm install
npx vite
```

## Build

```
npx vite build
```

Output goes to `dist/`.
