/**
 * Shared constants used across multiple modules.
 */

/**
 * Fraction of canvas height reserved as the selection zone (top strip).
 * In this zone: pointer cursor, slice selection on click, no marker placement.
 * The waveform drawing uses the same value for its top margin.
 */
export const SELECT_ZONE = 0.10;
